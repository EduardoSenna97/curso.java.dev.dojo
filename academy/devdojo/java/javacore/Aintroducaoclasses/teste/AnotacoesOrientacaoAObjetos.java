package academy.devdojo.java.javacore.Aintroducaoclasses.teste;

public class AnotacoesOrientacaoAObjetos {

    /*

    Coesão: coesão é algo que está relacionado ao propósito das minhas classes;
    Quando digo que um código é altamente coeso, significa que elas não estão misturando o propósito delas
    de existirem. Quanto menos propósitos uma classe, melhor.

    Significa que não posso misturar as classes. Exemplo: se eu colocar uma classe professor dentro da classe estudante.
    Isso vai misturar. Ou seja, devo criar uma nova classe fora a do estudante.
    Não posso misturar os conceitos de coisas DIFERENTES e nem colocá-los no mesmo objeto!

    Isso se aplica ao mesmo fato de colocar uma classe Main dentro da classe Estudante; quando
    eu for alterar a classe Estudante, é bem provável que vá causar alterações na classe Main também.

    ->Classe é uma representação abstrata de algo do mundo real. Ela serve como molde (ou estrutura) para criar objetos.


    Exemplo: A classe Pessoa representa uma pessoa em geral e pode gerar vários
    objetos Pessoa, como Maria, João, Ana etc.

    ->Classes de Domínio vs Classe com main
    As classes de domínio são aquelas que modelam conceitos do mundo real (como Pessoa, Carro, Produto).
    Elas não têm o método main.
    Elas servem apenas para representar dados e comportamentos.

    Já a classe com o método main é aquela que inicia a execução do programa.
    É o ponto de entrada da aplicação Java.

    -> O METODO MAIN é obrigatório em programas Java que vão ser executados diretamente.
    Dentro dele você pode instanciar objetos e chamar métodos das outras classes (como as de domínio).

    Classe → modelo/estrutura para criar objetos.
    Objeto → instância concreta da classe.
    Classe de domínio → representa algo do mundo real, não possui main.
    Classe com main → ponto de entrada da aplicação, onde a execução começa.

    Parâmetros -> Dar dados para que os métodos executem o que eles queiram. Parâmetros são variáveis locais, vivas;
    durante o contexto do método. Ele é parâmetro quando eu coloco dentro do método, porém ao
    executar o método, ele se chama ARGUMENTO.

    REGRA IMPORTANTE -> Se eu tenho um método com mais de 3 argumentos, significa que meu método/
    função está fazendo muita coisa.












     */



}
